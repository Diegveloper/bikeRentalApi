version: '3.1'

services:
  db:
    image: postgres
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_PASSWORD: diego
    volumes:
      - ./src/main/resources/postgres/schema.sql:/docker-entrypoint-initdb.d/schema.sql
